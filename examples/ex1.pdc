module example

export simple-rule
export missing
export simple-rule

rule simple-rule(A, B) {
	A -> B : DD
}

rule simple-rule(A, B) {
    A -> B : REQ
    many-of {
        B -> A : WHUT
        B -> A : WAWA
    }
    B -> A : RES
}
